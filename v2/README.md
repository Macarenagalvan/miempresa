# Journal V2 — Slice 0

No es el Journal V1. V1 sigue en la raíz del repo (`/index.html`).

## Cómo abrir

Desde la raíz del repo:

```
python3 -m http.server 8080
```

Después:

- App: http://localhost:8080/v2/
- Tests: http://localhost:8080/v2/tests/

No usar `file://`.
